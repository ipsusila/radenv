#ifndef KMATH_H
#define KMATH_H

#include "radglobal.h"

class K_CORE_EXPORT KMath
{
public:

    static qreal i0(qreal x);
    static qreal k0(qreal x);
    static qreal pr(qreal x);
};

#endif // KMATH_H
